---
import Navbar from '../components/sidebar.astro';
import '../styles/global.css'
---
<body>
  <Navbar />
  <main>
    <div>
      <h1>THANK YOU FOR YOUR TIME!</h1>
      <div class="contacts">
      <a href="mailto:limweijen96@gmail.com" style="text-decoration: underline;" target="_blank"><h2>Gmail</h2></a>
      <a href="https://github.com/LimWeiJen" target="_blank"><h2>Github</h2></a>
      <a href="https://discord.com/users/974579792248700938" target="_blank"><h2>Discord</h2></a>
      <a href="https://www.instagram.com/limweijen96/" target="_blank"><h2>Instagram</h2></a>
      <div class="preferable">(preferred)</div>
      </div>
    </div>
   <div class="dots">
  </div>
 <script type="text/javascript">
  console.log('hello')
  const parallaxContainer = document.querySelector('.dots');
  const speedList = [];

  for (let i = 0; i < 50; i++) {
    const dots = document.createElement('div');
    dots.classList.add('dot');
    dots.style.width = '0.2rem';
    dots.style.height = '0.2rem';
    dots.style.borderRadius = '100%'
    dots.style.background = 'rgb(255, 255, 255, 0.1)';
    dots.style.animation = 'fade 4s forwards ease-in'
    dots.style.zIndex = '-10';
    dots.style.position = 'absolute';
    dots.style.left = `${Math.random() * 100}%`;
    dots.style.top = `${Math.random() * 100}%`;
    parallaxContainer.appendChild(dots);
    speedList.push(Math.random() * 2);
  }

document.addEventListener('mousemove', handleMouseMove);

function handleMouseMove(event) {
  const mouseX = event.clientX;
  const mouseY = event.clientY;

  const offsetX = mouseX / parallaxContainer.offsetWidth;
  const offsetY = mouseY / parallaxContainer.offsetHeight;

  const parallaxLayers = document.querySelectorAll('.dot');

  parallaxLayers.forEach((layer, i) => {
    const translateX = offsetX * 100 * speedList[i];
    const translateY = offsetY * 100 * speedList[i];

    layer.style.transform = `translate3d(${translateX}px, ${translateY}px, 0)`;
  });
}
</script>

 </main>
</body>

<style>
  .preferable {
    color: #fff;
    position: absolute;
    transform: rotate(-15deg) translateY(-3.5rem) translateX(-2rem);
    font-size: 0.8rem;
  }
  a {
    transition: all 0.3s ease;
  }
  a:hover {
    transform: scale(1.1);
  }
  .contacts {
    display: flex;
    justify-content: space-between;
  }
  body {
    overflow: hidden;
    opacity: 0%;
    animation: fade-in 3s ease forwards;
  }
  div {
    width: 30rem;
  }
  @keyframes fade-in {
    0% {
      opacity: 0%;
    }
    100% {
      opacity: 100%;
    }
  }
  .dots {
    position: absolute;
    top: 0;
    left: 0;
    width: 98vw;
    height: 97vh;
    overflow: hidden;
    z-index: -1;
  }
  @media (max-width: 767px) {
    .contacts {
      display: flex;
      flex-direction: column;
      align-items: center;
    }
    div {
      width: 20rem;
    }
    h1 {
      text-align: center;
    }
    .preferable {
      transform: rotate(-15deg) translateX(6rem);
    }
  }
<style/>
